@* weak-typed view, b/c the view is unaware of the data being received *@
@{
    if (DateTime.Now.Month == 9)
    {
        Layout = "_AugustLayout";
    }
}

<h2>Pizza List</h2>
<ul>
    @foreach(var p in @ViewBag.PizzList)
    {
        <li>@p</li>
    }
</ul>